spring:
  application:
    name: hello-world
server:
  port: 0  # Iniciará a aplicação e uma porta aleatória
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance.id:${random.value}}